Go codebase with standard Go conventions:
- Use `gofmt` for formatting.
- Tests live in `*_test.go` (e.g., `markdown/link_test.go`).
- Package names are lower-case, short, domain-focused.
- No explicit lint config found; rely on Go tooling defaults.